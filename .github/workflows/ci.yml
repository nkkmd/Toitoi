name: CI

on:
  push:
  pull_request:

env:
  # Opt into Node.js 24 runtime for JavaScript-based GitHub Actions ahead of the default switch (June 2nd, 2026).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          # pnpm@11 requires Node >= 22.13
          node-version: 24
          package-manager-cache: false

      - name: Enable Corepack
        run: corepack enable

      - name: Activate pnpm
        run: corepack prepare pnpm@11.3.0 --activate

      - name: Get pnpm store path
        id: pnpm-store
        run: echo "STORE_PATH=$(corepack pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-store.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: corepack pnpm install --frozen-lockfile

      - name: Validate v0.8.0 protocol vocabulary
        run: node packages/protocol/test_vocabulary.js

      - name: Validate v0.8.0 search core
        run: |
          node apps/api/test_fts5_search_projection.js
          node apps/api/test_search_result_classifier.js
          node apps/api/test_v0_8_0_search_api.js
          node apps/api/test_v0_8_0_search_reference_dataset.js

      - name: Validate v0.8.0 reuse and replay
        run: |
          node apps/api/test_vocabulary_http_service.js
          node apps/api/test_related_inquiry_api.js
          node apps/api/test_search_replay_equivalence.js

      - name: Validate v0.8.0 frontend exploration
        run: |
          node apps/frontend/test_search_exploration_view_model.js
          node apps/frontend/test_search_exploration_renderer.js

      - name: Run tests
        run: corepack pnpm test
