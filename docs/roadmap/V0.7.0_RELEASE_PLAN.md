# Toitoi v0.7.0 Release Plan

**Status: released** | **Baseline: v0.6.0** | **Started: 2026-07-21** | **Implementation completed: 2026-07-21** | **Merged: 2026-07-21** | **Released: 2026-07-21** | **Last updated: 2026-07-21**

## Release intent

v0.7.0 turns inquiry lineage from a read-only inspection capability into a first-class authoring workflow. A user can derive a new Inquiry Draft from an existing published inquiry, explicitly select and explain the semantic relation, complete human review, publish through the existing Canonical Event and multi-transport boundaries, and trace the result back through provenance and lineage.

## Completed scope

1. First-class semantic relation vocabulary:
   - `derived_from`
   - `translated_from`
   - `observed_alongside`
   - `contrasts_with`
   - `synthesizes`
   - `reframes`
   - `annotates`
   - `revises`
2. Relation-specific authoring guidance and validation.
3. Creation of a derived Inquiry Draft from an existing inquiry.
4. Explicit separation of human-confirmed relations from AI-suggested relations.
5. Standard API workflow mutation for derivation.
6. SPA/PWA API client and mobile authoring support for derivation.
7. Publication metadata containing source inquiry, semantic relation, author, AI involvement, and human review.
8. Deterministic tests covering relation contracts and the derivation-to-publication workflow.
9. Lineage reconstruction after transport projection, re-ingest, and canonical storage replay.
10. Integrated lineage, context, provenance, identity, AI, and human-review presentation.
11. Operator, frontend, release, and upgrade documentation.

## Semantic invariants

- Canonical identity, semantic relation, and context similarity remain separate concepts.
- Similarity never causes automatic identity merge.
- A relation is not authoritative until a human explicitly confirms it.
- AI may suggest a relation, but the selected relation and confirmation state are recorded independently.
- A derived Inquiry Draft follows the existing `draft → in_review → approved → published` boundary.
- Publication remains impossible without independent human approval.
- Transport delivery failure does not erase the approved Canonical Event.

## Relation validation

| Relation | Required authoring signal |
|---|---|
| `derived_from` | General derivation note is optional |
| `translated_from` | Target language must differ from the source language |
| `observed_alongside` | Context or observation note is required |
| `contrasts_with` | Contrast rationale is required |
| `synthesizes` | At least two distinct source inquiry IDs are required |
| `reframes` | Reframing rationale is required |
| `annotates` | Annotation text or rationale is required |
| `revises` | Revision summary is required |

## Implemented HTTP boundary

```text
POST /api/v1/inquiries/:id/derive
```

The mutation creates an Inquiry Draft only. It does not bypass submit, independent review, approval, or publication guard operations.

## Golden Path

```text
published inquiry
  → choose derive action
  → choose semantic relation
  → complete relation-specific fields
  → explicitly confirm the relation
  → create derived Inquiry Draft
  → submit for review
  → independent human approval
  → publish distinct Canonical Event
  → inspect provenance and lineage
  → project to transport and re-ingest
  → replay canonical storage
  → recover the same lineage semantics
```

## Acceptance criteria

- [x] All eight relation types are accepted by the protocol contract.
- [x] Invalid relation-specific input is rejected deterministically.
- [x] A user can create a derived Inquiry Draft from an existing inquiry through the workflow HTTP boundary.
- [x] Human-confirmed and AI-suggested relation state remain distinguishable.
- [x] Existing Draft review and publication guard remain mandatory.
- [x] Published events preserve source inquiry IDs and semantic relation metadata.
- [x] Lineage tree can trace a published derivation to its source inquiry.
- [x] Transport projection, re-ingest, and replay restore equivalent lineage.
- [x] Canonical identity, semantic relation, and context similarity are presented separately.
- [x] Mobile-first frontend can invoke the derivation workflow.
- [x] v0.6.0 and earlier tests remain green in CI.

## Validation assets

- `packages/protocol/test_derived_inquiry.js`
- `apps/api/test_v0_7_0_inquiry_derivation.js`
- `apps/frontend/test_derivation_form_model.js`
- `apps/frontend/test_genealogy_view.js`
- `apps/frontend/test_v0_7_0_transport_replay.js`
- existing v0.3.0 through v0.6.0 Golden Path and regression tests

## Final validation

- PR #35 final CI run #525: success
- complete workspace test step: success
- v0.6.0 and earlier regressions: success
- implementation PR head: `e9ad39792309df38f7e0b7509a7ede9b0fb24e14`
- implementation merged to `main` as `b1ba070c1bf9cfc4c21be74a4cd9d4e479640959`
- PR #35 merged on 2026-07-21
- release tag `v0.7.0` published on 2026-07-21
- release tag target: `79013aad304c0778f49dab67678cbf0ef6ee9fd0`
- GitHub Release published as `Toitoi v0.7.0 — Inquiry Derivation and Genealogy`

The repository workflow currently validates pull-request heads. No separate push-triggered workflow run was produced for the squash merge commit; the merged tree corresponds to the successful PR candidate plus release-record synchronization.

## Explicitly out of scope

- semantic search, embeddings, vector search, or RAG
- automatic identity merge
- graph inference
- large-scale graph visualization
- real-time collaborative editing
- production multi-user authorization
- AI-selected relations without human confirmation
- new transport implementations

## Architecture decision

v0.7.0 extends the existing `@toitoi/protocol → workflow service → workflow HTTP service → SPA/PWA` path. It does not introduce a new service, persistence layer, transport, identity model, or publication workflow.

## Release administration

Completed on 2026-07-21:

1. merged PR #35
2. recorded the implementation merge SHA
3. validated the release candidate through CI run #525
4. created tag `v0.7.0` at `79013aad304c0778f49dab67678cbf0ef6ee9fd0`
5. published the GitHub Release
6. synchronized release records to `released`
