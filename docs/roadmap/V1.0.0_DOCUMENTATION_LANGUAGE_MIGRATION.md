# v1.0.0 Documentation Language Migration

**Status: active release gate**  
**Policy:** `docs/governance/DOCUMENTATION_LANGUAGE_POLICY.md`  
**Target release:** v1.0.0  
**Last updated:** 2026-07-23

## Objective

Bring the maintained Toitoi documentation into a deliberate language structure before v1.0.0:

- major public and operational documents are bilingual, with English followed by equivalent Japanese;
- detailed technical and implementation documents are English;
- historical records retain their existing language unless they create a current misunderstanding.

This migration does not require translating every existing file. It requires making the maintained document set intentional, navigable, and safe for release.

## Release-blocking scope

The following work must be complete before v1.0.0 publication.

### A. Policy and navigation

- [x] publish the normative documentation language policy;
- [ ] link the policy from the root README and contributor documentation;
- [ ] identify each Class A document in repository navigation;
- [ ] ensure no Class A path points to a missing file;
- [ ] document the exception process for non-semantic one-language changes.

### B. Class A bilingual documents

| Document | Current state | Required v1.0.0 action | Release blocking |
|---|---|---|---|
| `README.md` | bilingual | add synchronization markers and policy link; verify semantic parity | yes |
| designated architecture overview | fragmented across architecture documents | designate or create one bilingual overview | yes |
| `docs/protocols/CANONICAL_EVENT.md` | Japanese only | add complete English section before Japanese; retain normative wire details | yes |
| `docs/reference/V1.0.0_SETUP_AND_DEMO.md` | English only | add equivalent Japanese section | yes |
| `docs/operations/V1.0.0_OPERATIONS_RUNBOOK.md` | missing | create minimum bilingual operations runbook | yes |
| `CONTRIBUTING.md` | inventory pending | make contributor entry point bilingual and link policy | yes |
| designated security entry point | inventory pending | create or update bilingual security and sensitive-information entry point | yes |
| `docs/roadmap/RELEASE_NOTES.md` | inventory pending | ensure public v1 release entry is bilingual; historical entries may remain unchanged | yes |

### C. Minimum operations runbook content

The bilingual v1 runbook must include:

- prerequisites and supported reference topology;
- initial setup and persistent directory layout;
- start, stop, and restart;
- liveness and readiness checks;
- authentication reference mode and authority boundaries;
- backup creation and manifest verification;
- restore and corruption verification;
- migration plan, dry-run, apply, and rollback decision boundary;
- durable queue inspection, retry, and dead-letter recovery;
- transport pending, failed, and quarantined states;
- AI provider unavailable or disabled behavior;
- credential rotation and secret-handling boundary;
- upgrade procedure;
- rollback procedure;
- incident evidence and audit-log preservation;
- explicit distinction between durable and rebuildable state.

### D. Consistency checks

- [ ] commands, environment variables, endpoint paths, package filters, versions, and file paths match across language sections;
- [ ] Canonical Event `schemaVersion: "0.1.0"` remains distinct from repository release `v1.0.0`;
- [ ] `synthesizes` remains the normative synthesis lineage relation;
- [ ] human review and publication authority boundaries are identical in both languages;
- [ ] no translated text implies that transport delivery, AI output, or similarity establishes Canonical identity or agronomic correctness;
- [ ] language synchronization dates are present;
- [ ] automated documentation-language checks run in default CI.

## Non-blocking Class B normalization

The following should be English, but individual remaining Japanese passages do not block v1.0.0 unless they conflict with a Class A contract or operation:

- transport-specific specifications;
- package-internal READMEs;
- adapter and converter implementation documents;
- fixture and test explanations;
- release plans, implementation records, and final-review records;
- CI and deployment internals;
- detailed API endpoint reference;
- pilot internal collection and analysis templates.

For these documents:

- new text should be written in English;
- mixed-language files should be normalized when materially edited;
- broad mechanical translation without technical review should be avoided.

## Class C historical documents

Historical documents retain their existing language and structure.

A Class C file is changed only when:

- it is incorrectly presented as current;
- it points to a removed or unsafe procedure;
- it conflicts with a current normative contract;
- its language causes a material release or operational misunderstanding.

## Work sequence

### Phase 1 — governance and inventory

1. Publish the language policy.
2. Publish this migration register.
3. Add links from README, release runbook, and contributor entry point.
4. Confirm the exact Class A file paths.

### Phase 2 — operationally critical bilingual documents

1. Create `docs/operations/V1.0.0_OPERATIONS_RUNBOOK.md`.
2. Translate and synchronize `docs/reference/V1.0.0_SETUP_AND_DEMO.md`.
3. Translate and synchronize `docs/protocols/CANONICAL_EVENT.md`.
4. Designate or create the bilingual architecture overview.

### Phase 3 — public entry and governance documents

1. Synchronize `CONTRIBUTING.md`.
2. Synchronize or create the security entry point.
3. Prepare the bilingual v1.0.0 release-notes entry.
4. Add synchronization metadata to all Class A files.

### Phase 4 — automated checks and final review

1. Add a Class A registry checker.
2. Check English and Japanese markers.
3. Check critical literals in both sections.
4. Run default CI.
5. Perform human semantic-equivalence review.
6. Record completion in `V1.0.0_RELEASE_RECORD.md`.

## Current execution status

Completed:

- normative policy created;
- migration register created;
- Class A candidate set defined;
- release-blocking and non-blocking work separated.

Next implementation steps:

1. update root documentation navigation and release gates;
2. create the bilingual minimum operations runbook;
3. synchronize setup/demo and Canonical Event documents;
4. add automated Class A checks;
5. run CI and update the release record.

## Release decision rule

The language migration gate passes when:

- all registered Class A documents exist;
- all release-blocking Class A documents contain English followed by equivalent Japanese;
- critical operational literals match;
- default CI passes;
- a human reviewer confirms no known material divergence.

Class B or Class C translation completeness is not a v1.0.0 release condition.
